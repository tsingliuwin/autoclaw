# 安全声明

[English](SAFETY.md) | 简体中文

## AutoClaw 能做什么

AutoClaw 会执行模型生成的 shell 命令、读写文件、访问网络、发送邮件并推送群通知。它为无人值守而设计(`-y -n`),这意味着**每条命令执行前没有人工审核**。

## 现状

AutoClaw 未经过安全审计。错误的模型输出、缺陷、配置错误或恶意输入(包括网页、仓库文件或工具结果中携带的提示词注入)可能损坏主机、修改或删除文件、泄露数据或凭据。

## 内置缓解措施 —— 及其边界

| 措施 | 做什么 | 不做什么 |
| --- | --- | --- |
| 危险命令闸 | 即使 `-y` 也拒绝明显破坏性命令(`rm -rf`、`format`、`shutdown` 等) | 不是完备黑名单,新型破坏性命令可能通过 |
| 敏感路径防线 | `read_file`/`write_file` 拒绝 AutoClaw 自身凭据存储与 `.env*` 文件 | `execute_shell_command` 仍可读取这些路径——防线提高门槛,不是硬边界 |
| 步数上限 / 墙钟超时 | 限制运行的时长与深度 | 不判断预算内发生了什么 |
| 工具结果修剪 | 防止超长输出淹没模型上下文 | 不净化内容 |
| 运行日志 | 记录发生的一切供事后排查 | 只记录在本地,不是审计服务 |

`--allow-dangerous` 会关闭危险命令闸。带此参数运行时,只有当你同时信任任务本身以及任务所能读到的所有内容时,才视为可信。

## 负责任的使用

- 以任务所需的最小权限和文件访问范围运行;对不可信工作负载,优先使用一次性容器或虚拟机。
- 对 AutoClaw 可写的任何内容保留备份。
- `~/.autoclaw/setting.json` 里只放任务需要的凭据(每项集成都是可选的)。
- 无人值守运行后查看 `~/.autoclaw/logs/runs.jsonl`。
- 启动前可用 `autoclaw doctor` 检查实际生效的配置。

## 免责声明

AutoClaw 基于 MIT 许可证提供,不附带任何形式的保证。使用风险由你自行承担。
